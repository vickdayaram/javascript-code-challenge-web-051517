class Comment {

  constructor(text) {
    this.text = text
  }


  // added this method below after reading the instructions
  // in the lab but did not implement since I ran out of time.
  render() {
    return `<li> ${this.text} </li>`
  }

}
